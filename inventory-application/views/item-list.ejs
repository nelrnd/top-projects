<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | MUSIC STORE</title>
  <link rel="stylesheet" href="/styles/style.css" />
</head>
<body>
  <%- include("navbar") %>
  <h1><%= title %></h1>

  <a href="/item/new">Create a new item</a>

  <% if (item_list && item_list.length > 0) { %>
    <ul>
      <% item_list.map((item) => { %>
        <li><a href="/item/<%= item.id %>"><%= `${item.name} by ${item.brand_name} (${item.category_name})` %></a></li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No items for now...</p>
  <% } %>
</body>
</html>